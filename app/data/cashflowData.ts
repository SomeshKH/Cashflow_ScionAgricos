/**
 * cashflowData.ts
 *
 * All business data is now served from the backend API (PostgreSQL + Excel ingestion).
 * This file is intentionally empty — do not add hardcoded data here.
 *
 * See: app/services/api.ts         for all data-fetching methods.
 * See: backend/src/services/       for backend analytics queries.
 */
